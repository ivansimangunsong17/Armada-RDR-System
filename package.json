{
  "name": "running-discharge-system-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:fe": "npm --prefix frontend run dev -- --host 127.0.0.1",
    "dev:be": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "start:backend": "npm --prefix backend start",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "db:backup": "npm --prefix backend run db:backup",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:schema": "npm --prefix backend run db:schema",
    "db:seed-admin": "npm --prefix backend run db:seed-admin",
    "install:all": "npm --prefix frontend install && npm --prefix backend install"
  }
}
